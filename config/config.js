//TODO move DB cred's into module and change secret or randomly generate a new one for each user 
//module to be exported into server for token signin & DB cred's
module.exports = {

    'secret': 'keepthisasecret',

};